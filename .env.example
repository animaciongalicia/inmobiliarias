# Make Webhook URLs
# Copy this file to .env.local and fill in your actual webhook URLs.
# These values are NEVER exposed to the browser — only read server-side in /api/submit.

# Default webhook (used for the DEFAULT agency and as fallback)
MAKE_WEBHOOK_DEFAULT=

# Agency-specific webhook for coruna01
MAKE_WEBHOOK_CORUNA01=
